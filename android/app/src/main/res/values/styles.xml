<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- GreenFlag has no light mode: the app is always dark (bg-base is
         pure black). DayNight let the system bars follow the device's
         light/dark setting, so on a phone set to light mode Android drew
         its default light-gray navigation bar under our black content,
         the one visibly mismatched strip in an otherwise fully dark UI.
         Forcing plain AppCompat.NoActionBar (not DayNight) plus explicit
         black system bar colors and disabled light-bar icon flags keeps
         it black with light icons regardless of the device's setting. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- Without this, nothing ever tells Android to switch away
             from the splash theme once real content is ready. The app
             was stuck permanently under Theme.SplashScreen's system-bar
             styling (forced black), showing as a persistent black strip
             near the system nav bar on every screen. See MainActivity's
             installSplashScreen() call, which is what actually triggers
             this transition. -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>